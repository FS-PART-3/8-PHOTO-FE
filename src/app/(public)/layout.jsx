// 퍼블릭 라우트 레이아웃 (로그인 불필요)
export default function PublicLayout({ children }) {
  return (
    <div className="public-layout">
      {/* 퍼블릭 레이아웃 추가될 예정 */}
      <main>{children}</main>
      {/* 퍼블릭 레이아웃 추가될 예정 */}
    </div>
  );
}
