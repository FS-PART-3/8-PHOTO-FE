// 랜덤 포인트 컴포넌트
export default function RandomPoint({ points }) {
  return (
    <div className="random-point">
      {/* 랜덤 포인트 표시 로직이 여기에 추가될 예정 */}
      <span>포인트: {points}</span>
    </div>
  );
}
