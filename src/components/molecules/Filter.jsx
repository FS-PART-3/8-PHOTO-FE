// 필터 컴포넌트
export default function Filter({ filters, activeFilter, onFilterChange }) {
  return (
    <div className="filter">
      {/* 필터 로직이 여기에 추가될 예정 */}
      <span>필터: {activeFilter}</span>
    </div>
  );
}
